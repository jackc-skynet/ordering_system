# Change Set Summary: v1.3.1 - Order Tracking UI Visual Refinements

**Date**: 2026-03-19
**Version**: v1.3.1

## Affected Files
- `src/App.jsx`: Updated `OrderTrackingView` with dynamic icons, titles, and corrected progress bar scaling.
- `src/index.css`: Added `.pulse` animation for the "Preparing" state.

## Key Changes
- **Visual Feedback**: The order tracking screen now uses specific icons to reflect the current phase:
    - `pending` -> `Clock`
    - `preparing` -> `ChefHat` (with pulse animation)
    - `ready` -> `Utensils`
- **Dynamic Headings**: Replaced the static "Chef is serving you" with context-aware titles (e.g., "主廚正全力製作中...", "餐點好囉！請前往取餐").
- **Accurate Progress Bar**: Corrected the CSS width calculation from `40%` to `50%` to ensure the 3-stage progress dots are connected perfectly.

## Commit Message
`v1.3.1 - Fix: Add dynamic icons, headings, and correct progress bar scaling to OrderTrackingView`
